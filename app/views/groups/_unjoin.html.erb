<%= form_with(model: current_user.active_relations.find_by(group_id: @group.id),
            html: { method: :delete }, local: true) do |f| %>
  <%= f.submit "Leave", class: "btn btn-secondary" %>
<% end %>