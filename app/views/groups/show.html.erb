<% provide(:title, @group.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="group_info">
      <h1>
        <%= @group.name %>
      </h1>
    </section>
    <section class="stats">
      <%= render 'shared/stats_user' %>
    </section>
  </aside>
  <div class="col-md-8">
    <%= render 'join_form' %>
  </div>
