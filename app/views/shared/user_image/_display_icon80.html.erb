<%# ユーザーの画像を表示するためのパーシャル(80×80) %>
<% if user.image? %>
  <%= image_tag user.image.thumb80.to_s, alt: "user-icon", class: "user-icon" %>
<% else %>
  <i class="fas fa-user user-icon font-awesome-80"></i>
<% end %>