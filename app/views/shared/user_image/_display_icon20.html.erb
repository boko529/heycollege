<%# ユーザーの画像を表示するためのパーシャル(80×80) %>
<% if user.image? %>
  <%= image_tag user.image.thumb20.url, alt: "user-icon", class: "user-icon" %>
<% else %>
  <%= image_tag 'default_user.png', alt: "user-icon", :size => '20x20', class: "user-icon" %>
<% end %>