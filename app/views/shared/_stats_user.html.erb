<% @group ||= current_group %>
<div class="stats">
  <%# <a href="<%= users_group_path(@group) %>
    <span id="users" class="stat">
      <%= t('.members') %>：<%= @group.users.count %>
    </span>
  </a>
</div>