<%# ユーザーの画像を表示するためのパーシャル(80×80) %>
<% if group.profile_image? %>
  <%= image_tag group.image.thumb80.url, alt: "group-icon", class: "group-icon" %>
<% else %>
  <%= image_tag 'default_user.png', alt: "group-icon", :size => '80x80', class: "group-icon" %>
<% end %>